interface:
  display_name: "Orynth Architecture Guard"
  short_description: "Protege fronteiras entre camadas"
  default_prompt: "Use $orynth-architecture-guard para validar se uma feature ou mudança do Orynth respeita as fronteiras entre frontend, backend, banco, jobs, comunicações e admin."

policy:
  allow_implicit_invocation: true
