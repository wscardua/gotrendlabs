# Status de Implementação

| Feature | Status spec | Status impl | Última atualização | Observação |
|---|---|---|---|---|
| FEAT-MOBILE-001 | draft | parcial | 2026-06-13 | Projeto Flutter mobile criado em `apps/mobile`, com shell dark-first/editorial, bottom navigation, feed/browse/busca/detalhe via FastAPI, auth Bearer simples em secure storage com `Lembrar login` ligado por padrão e modo sem persistência em storage, proteção local ligada por padrão para sessão lembrada quando o aparelho suporta biometria/senha local antes de ativar o token persistido em memória, gate de manutenção mobile via `GET /health` enriquecido e `X-GoTrendLabs-Client: mobile`, tela dark-first para backend indisponível/manutenção e sem excecao por papel no app, favoritos/curtidas/comentários/preview/criação de previsão por endpoints backend, wallet/perfil/ranking/badges/alertas como leitura da API, recarga controlada via fila Admin Ops, filtros de ranking por categoria/subcategoria/evento, ranking mobile por `@handle` com badges compactas e overflow `+N`, recortes pessoais `Todos`/`Favoritos`/`Posições` em Mercados baseados em flags autenticados da FastAPI, atalho `Sua mesa` em Hoje, tela `Hoje` com destaque/tendências apenas de mercados abertos ordenados por engajamento visual, cards com prazo compacto e barra de urgência com cor dinâmica, detalhe com hero não navegável, tracking de `view_count` ao abrir detalhe e consenso multi-série por opção via `sparkline_series`, tela `Sobre` com versão/build/saúde da API/estado informativo de push/diagnóstico seguro sem expor endpoints, páginas mobile de política/conceitos/segurança, imagens de mercado/badges resolvidas por contrato, mensagens de erro finais sem payload técnico e refresh visual aplicado ao design system, telas principais, secundárias e bottom sheets com confirmação de previsão rolável/segura; identidade nativa atualizada para nome `GoTrendLabs`, icones iOS/Android derivados do logo do site, variantes iOS `dark`/`tinted` e splash Android escuro com lockup da marca/tagline alinhada; alvo Android validado em emulador, estrutura iOS em `apps/mobile/ios` preparada e app executado no iPhone 17/iPhone 17 Pro Max Simulator com bases locais via `127.0.0.1`; push mobile possui repository/controller, modo local `FakePushTokenProvider` via `GTL_PUSH_FAKE_TOKEN`, Firebase/FCM Android opcional por `google-services.json` local, token real registrado apenas após autenticação, permissão `POST_NOTIFICATIONS`, canal Android `gtl_default`, abertura segura de rotas por payload e revogação explícita via FastAPI; canal beta Android direto pelo site foi implementado com signing release local obrigatório, Admin Ops de APK, CTA direto no rodape, nas telas de acesso e nas paginas de compartilhamento, JSON `/app/android/latest.json`, storage em media e `/api/*` de produção roteado para FastAPI, com APK `1.0.5 (6)` publicado após manutenção mobile independente e smokes públicos de API/download validados; app Android em emulador registrou `PushDevice` fake autenticado para QA local; `flutter doctor -v`, `flutter analyze`, `flutter test`, build Android debug/release, OpenAPI check, `manage.py check` e testes focados validados conforme fatias. Fluxo autenticado real amplo, QA visual completo, TestFlight/App Store, Google Play, teste ponta a ponta de recebimento FCM em dispositivo real, refresh token, auto-update in-app e offline seguem fora desta entrega |
| FEAT-AUTH-001 | draft | parcial | 2026-06-11 | FastAPI auth/session com aceite de política, política pública/modal no cadastro, reCAPTCHA v2 opcional no cadastro, cadastro com `referral_code` opcional preservado por `?ref=`, navegação pública, rodapé público institucional sem links de conta/admin, Admin Ops no chip apenas para staff/superuser, retorno `← Voltar` com origem local confiável e fallback para feed, login social real para Google/Facebook/X com vínculo seguro por identidade externa/email verificado e conclusão por email informado com token pendente assinado quando o provedor não retorna email, opção de lembrar sessão no login, edição inline de perfil com prefixo `@` fixo no identificador e dados privados opcionais vindos de `gotrendlabs_user_profiles`, confirmação de email por token expirável, login limitado até confirmação, reenvio limitado de confirmação, recuperação de senha enviada por outbox sem expor `reset_url` na resposta pública, emails críticos de auth com tentativa imediata e fallback para daemon, redirects `next` restritos a origem local, rate limit in-memory para endpoints públicos sensíveis, hardening automático de cookies/headers em modo produção, exclusão lógica, gestão administrativa de usuários/sessões/badges adquiridas, marcação `is_bot` restrita ao admin e geração administrativa auditada de link de reset de senha; home exibe `Sua progressão` para operadores com estado neutro quando fora do ranking público; Django consome a API |
| FEAT-MARKET-001 | draft | parcial | 2026-06-07 | Feed público vem da FastAPI/Postgres, com home simplificada para priorizar mercados, cards, títulos clicáveis para detalhe, tags de categoria/subcategoria/evento, fechamento formatado sem ISO cru, mini gráficos, indicador horizontal de prazo calculado por `created_at`/`close_at`, destaque principal por `view_count` excluindo `draft`/`canceled`, ticket de onboarding por `view_count` excluindo `draft`/`canceled`, fallback visual de thumbnail, filtro público de `image_url` local inexistente, ordenações rápidas client-side com rótulos públicos `Em alta`, `Abertos`, `Em apuração`, `Resolvidos`, `Mais volume` e `Mais curtidas`, recorte de resolvidos, CTAs públicos por status (`Prever`, `Em apuração`, `Ver resolução`), favoritos acionáveis para usuário autenticado e affordance readonly para visitante, métricas públicas reais de previsões e economia educativa em `GT₵` excluindo créditos de operadores na distribuição, contadores operacionais de popularidade, taxonomia com avisos opcionais no Admin Ops, browse administrativo com busca textual e participantes, dashboard administrativo de contratos read-only em fases e trilho temporal para mercados ativos/pendentes, com legenda no topo, blocos de 10 e alerta de fechamento próximo/atrasado, exclusão segura de eventos sem mercados vinculados e fechamento automático centralizado no backend; migrations de produção não semeiam mercados fixture |
| FEAT-MARKET-002 | draft | parcial | 2026-05-22 | Detalhe público vem da FastAPI/Postgres com opções, consenso decimal, evolução por opção preservada após resolução, categoria/subcategoria/evento, avisos de categoria/subcategoria/evento abaixo do critério de resolução quando preenchidos, ação de favorito autenticada com affordance readonly para visitante, ticket de resultado personalizado, compartilhamento social de pergunta/resultado com opções/probabilidades e CTA para o detalhe, imagem social dinâmica e tracking operacional de visualização/compartilhamento |
| FEAT-PRED-001 | draft | parcial | 2026-06-14 | Previsão autenticada com stake, bloqueio de saldo, ledger, snapshot decimal de probabilidade, `/predict` reservado à primeira previsão, reforço/revisão web-first por contratos FastAPI próprios, múltiplas posições auditáveis em `gotrendlabs_predictions`, limite configurável de reforços, supersedência de posições revisadas, custo `prediction_revision_penalty`, parâmetros de operação agrupados em Admin Ops Config, detalhe web com resumo das entradas abertas antes da revisão, ticket sem opção pré-selecionada com escolha obrigatória nativa, estado sem saldo disponível, prévias sem efeito colateral via FastAPI e séries visuais derivadas de posições persistidas; mobile ainda consome apenas previsão inicial até fase posterior |
| FEAT-RES-001 | draft | parcial | 2026-05-20 | Resolução manual com opção vencedora, timestamp/timezone controlados, payout/reputação pela fórmula MVP, perda auditável, cancelamento com refund total, validação contra previsões abertas órfãs, reconciliação operacional idempotente, desfazer resolução para fechado, `MarketLifecycleEngine` centralizada e auditoria staff read-only de distribuição de ledger/badges no Admin Ops |
| FEAT-REP-001 | draft | parcial | 2026-06-13 | Reputação base, ranking global/temático/evento via FastAPI, ranking web com `Carregar mais` em blocos de 10 linhas, badges conquistadas compactas após o handle, exclusão de admins/bots no backend, atualização por resolução usando `K=10`, badges administráveis com exibição histórica separada da concessão automática, pausa de novas concessões sem remover a badge do catálogo público/autenticado, ocultação operacional do catálogo por `is_active=false`, miniatura no browse Admin Ops, recorte opcional por categoria/subcategoria/evento e compartilhamento social por card/metadados |
| FEAT-WALLET-001 | draft | parcial | 2026-06-14 | Ledger auditável, projeção `gotrendlabs_wallet_balances`, grant inicial, stake lock, refund idempotente por cancelamento/reconciliação, payout, perda por resolução, revisão de posição com liberação de stakes antigos, débito `prediction_revision_penalty` e novo lock transacional, extrato com `Carregar mais` em blocos de 10 lançamentos, recompensas operacionais, indicação bonificada `reward_referral` com valor configurável e card contextual em carteira/perfil, recarga educativa por fila Admin Ops com piso configurável, ajuste manual auditado por staff inclusive na própria conta e agregado público de `GT₵ distribuídas` baseado em créditos do ledger de usuários comuns |
| FEAT-COMMENT-001 | draft | parcial | 2026-05-18 | Comentários reais em mercados com criação autenticada, listagem pública, like/dislike único por usuário e moderação staff por ocultar/restaurar |
| FEAT-SUGGEST-001 | draft | parcial | 2026-06-06 | Sugestões de mercado e feedback possuem persistência, envio autenticado/guest com reCAPTCHA v2 opcional para visitantes, link `Sugerir mercado` no topo público, categoria de sugestão carregada da taxonomia ativa administrada em Admin Ops com validação backend, fila real no Admin Ops, conversão em rascunho e crédito operacional idempotente; Admin Ops não executa fallback local mutável |
| FEAT-NOTIFY-001 | draft | parcial | 2026-06-12 | Configuração operacional não sensível de email persistida em banco via Admin Ops, com provider Resend ou SMTP genérico; Resend usa API HTTPS, `GOTRENDLABS_RESEND_API_KEY` fora do banco/Git/Admin Ops, domínio remetente verificado no dashboard e comando `send_resend_test_email`; emails transacionais recebem rodapé institucional automático customizável via `system.transactional_footer` e emails críticos de identidade/acesso tentam envio imediato com fallback para outbox/daemon; inbox in-app persistida em `gotrendlabs_user_notifications`, sino no topo com contador/dropdown/marcar lidas, contador público de comentários nos cards/detalhe e notificações sociais/sistêmicas; `communications` possui `Politica de Emails` e `Política de Push` no Admin Ops com templates/editáveis, fallback seguro visível ao operador, teste manual por `PushDevice`, aba de dispositivos sem token bruto, logs filtráveis de `EmailDelivery`/`PushDelivery`, saúde de email/push no Dashboard, daemon com retries/dry-run e emails v1; push mobile possui `PushDevice`, `PushEventPolicy`, `PushTemplate`, `PushDelivery`, `PushPreference`, endpoints autenticados, provider `none`/dry-run desligado por padrão, sender FCM real via Firebase Admin SDK quando credencial de ambiente estiver ativa, claim transacional curto/recuperação de `sending` antigo no daemon e Flutter Android com token FCM real após autenticação; event bus dedicado, digest real, teste ponta a ponta de recebimento FCM em dispositivo real, bounce/complaint webhooks Resend e editor visual avançado seguem pendentes |
| FEAT-I18N-001 | draft | parcial | 2026-06-05 | Rebrand profundo para `GoTrendLabs`, domínio `gotrendlabs.com.br`, moeda `GTL Credits`/`GT₵`, contratos técnicos `_gtl`, tabelas `gotrendlabs_*`, env vars `GOTRENDLABS_*`, assets GTL, favicon SVG público/Admin Ops, docs/deploy alinhados e validação local/cloud concluída; extração completa de strings para catálogos segue fora desta fatia |
| FEAT-OPSLOG-001 | draft | parcial | 2026-06-06 | Logs técnicos persistidos em `gotrendlabs_system_logs`, captura Django/FastAPI/logging Python e logs técnicos de segurança, redaction de segredos, retenção configurável no Admin Ops com default 90 dias e prune backend reutilizável por `created_at`, `Carregar mais` em blocos de 10 e consulta staff por identificador de usuário no Admin Ops; Dashboard Admin Ops consome resumo agregado staff de saúde operacional, status do daemon com limites configuráveis e indicador `Backend API` via `GET /health`; defaults de saúde do daemon são 7 minutos para `Atrasado` e 21 minutos para `Sem sinal`; topo Admin Ops possui layout próprio para evitar sobreposição da navegação; daemon possui serviço dedicado no Docker Compose de produção com intervalo de 300 segundos; infra AWS base possui CloudWatch Agent, log groups e alarmes mínimos de host/RDS; workflow de deploy em `main` agora faz preflight de OIDC/configuração GitHub e valida a identidade AWS assumida antes do SSM |
| FEAT-AIAGENT-001 | draft | parcial | 2026-06-06 | Agentes IA oficiais possuem app `agents`, configuração em `gotrendlabs_site_config`, auditoria em `gotrendlabs_ai_agent_actions` com retenção configurável, paginação/filtro por motivo/detalhe operacional com explicações de tipo/status/motivo e códigos técnicos preservados, ciclo isolado no daemon, comentários oficiais com selo, prompt com cautela factual versionado, ciclo de comentários com candidatos configuráveis e tentativas LLM limitadas, suporte técnico a previsão bot controlada por humanos mínimos/saldo/limites, métricas humano/bot, saúde técnica no Admin Ops e exclusão de bots de ranking/badges/reputação |

## Regras de atualização

- Alterações relevantes em uma feature devem atualizar também `feature-changelog.md`.
- Quando o comportamento mudar sem implementação equivalente, marcar `status_impl` como `defasada_pela_spec`.
