interface:
  display_name: "GoTrendLabs Architecture Guard"
  short_description: "Protege fronteiras entre camadas"
  default_prompt: "Use $gotrendlabs-architecture-guard para validar se uma feature ou mudança do GoTrendLabs respeita as fronteiras entre web, futuro mobile, backend, banco, jobs, comunicações, admin e a estrutura alvo apps/ops/tools."

policy:
  allow_implicit_invocation: true
