services:
  postgres:
    image: postgres:16-alpine
    container_name: orynth-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-orynth}
      POSTGRES_USER: ${POSTGRES_USER:-orynth}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-orynth_dev_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - ./docker/postgres/data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

